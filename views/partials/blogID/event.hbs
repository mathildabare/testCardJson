{{!--
Partial : Event ID
--}}

<section class="event">
    <div class="container">
        <div class="row event-details">
            <h2> {{ article.title }} </h2>
            <img src="{{article.img}}" alt="">
            <p>{{ article.description }} </p>
        </div>
</section>